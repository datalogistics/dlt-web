/*
 * Rest Services for Main
 * public/js/services/
 * MainService.js
 */

angular.module('MainService', []).service('Main', function($http) {
  
  this.loadMain = function(mainInfo) {
    // need to load main page info, stats, do initial data subscribe, etc.
  };
});
