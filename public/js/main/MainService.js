/*
 * Main Service
 * public/js/main/
 * MainService.js
 */

function MainService($http) {
  var service = {};
  service.loadMain = function(mainInfo) {
    // need to load main page info, stats, do initial data subscribe, etc.
  };
  return service;
}
